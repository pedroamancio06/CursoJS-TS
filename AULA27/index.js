//operação ternário
const nome = 'Douglas';
const usuario = nome == 'Pedro' ? 'Acesso permitido!' : 'Acesso negado!';

const pontosTime = 71;
const titulo = pontosTime >= 80 ? 'Vocês foram campeões!' : 'Vocês não foram campeões!';

console.log(usuario);
console.log(titulo);

